/* eslint-disable import/no-unresolved, import/prefer-default-export, quotes, semi */
// 覺得之後也許會有其他的 context，就先不用 default export

import { createContext } from "react";

export const AuthContext = createContext(null);
